# TD3 Gated-Duck Policy

Frozen TD3 checkpoint and exact training configuration used by the canonical
four-policy explanation run.

- Evaluation: deterministic actor mean.
- Teacher active during explanation: no.
- Checkpoint hash is frozen in
  `artifacts/explainability/four_policy/reproducibility_manifest.json`.
